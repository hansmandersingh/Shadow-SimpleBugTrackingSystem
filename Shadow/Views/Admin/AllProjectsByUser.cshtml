@model List<Shadow.Models.Project>
@{
    ViewBag.Title = "AllProjectsByUser";
}

<h2>All Projects By User</h2>

@if (Model != null)
{
    <ul>
        @foreach (var pro in Model)
        {
            <li>
                @pro.Name
            </li>
        }
    </ul>
}
